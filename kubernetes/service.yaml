apiVersion: v1
kind: Service
metadata:
  name: scraper-service
  labels:
    app: scraper-service
spec:
  selector:
    app: scraper-service
  ports:
  - port: 8080
    targetPort: 8080
  - port: 9050
    targetPort: 9050
